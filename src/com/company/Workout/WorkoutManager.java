public class WorkoutManager {

  public static initWorkoutManager() {
    while(1) {
      if(input == list){
        list(args);
      }
      else if (input == newowrkout) {
        
      }
      else if(input == exit){
        break;
      }
    }
  }

  // functions to 
  public void startWorkout(String filePath) {
    create a new workout session object.

    // WorkoutSession ws = new WorkoutSession(filePath);
    // ws.startWorkoutSession();
  }

  public void listWorkout() {
  }

  public void deleteWorkout(int index) {
    remove index th workout session record completely
  }

}